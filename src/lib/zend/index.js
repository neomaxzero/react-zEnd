import zFetch from './hooks/zFetch';
import zend from './hooks/zEnd';
import ZendProvider from './context/ZFetchProvider';

export { zFetch, zend, ZendProvider };

export default {
	zFetch,
	zend,
	ZendProvider,
};
